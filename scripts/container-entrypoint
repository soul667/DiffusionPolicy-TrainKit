#!/usr/bin/env bash
set -euo pipefail

# Allows docker --user $(id -u):$(id -g) without creating a matching image user.
export HOME="${HOME:-/tmp/container-home}"
mkdir -p "$HOME" 2>/dev/null || true

exec "$@"
